# Интернет‑магазин (учебный проект)

## Описание проекта


Это учебный проект:

1) интернет‑магазина, разрабатываемый в рамках курса. 

2) Веб‑приложение с блогом

Проект будет поэтапно дорабатываться на каждом занятии. 

В итоге должен получиться полноценный веб‑магазин и  Веб‑приложение с блогом.

Интернет‑магазин включает модели Product (товар) и Category (категория), админку, фильтрацию и поиск, миграции, 
фикстуры и кастомную команду для заполнения БД тестовыми данными. В форме добавлены валидация и стилизация.
Для сервиса интернет-магазина реализована функция аутентификации пользователей, чтобы боты не могли вмешиваться
в работу сервиса. Доступ к страницам, связанным с управлением продуктами, для анонимных пользователей закрыт. 
Только авторизованные пользователи могут просматривать, создавать, изменять и удалять продукты. 
Общедоступна только страница просмотра списка товаров.

Веб‑приложение с блогом  включает модели блоговой записи Blog, админку, фильтрацию и поиск, миграции

###

## Структура проекта

manage.py — управляющий скрипт Django.

<home_work_shop>/ — корневая папка проекта (настройки, URL‑конфигурация).

blogs/                             - Приложение «Blogs» (основной функционал блога)

    admin.py                       - Настройка админ‑панели для моделей приложения

    apps.py                        - Конфигурация приложения (метаданные, регистрация)

    models.py                      - Описание моделей (структура таблиц в БД)

    tests.py                       - Юнит‑тесты для приложения

    urls.py                        - Маршрутизация URL для приложения «Blogs»

    views.py                       - Логика обработки HTTP‑запросов (представления)



    migrations/                   - Папка с миграциями (скрипты для синхронизации моделей с БД)

    0001_initial.py               - Начальная миграция (создание таблиц)

    0002_alter_blog_counter_vi... - Миграция для изменения полей моделей

    templates/                    - Папка с HTML‑шаблонами для приложения

    blogs/                        - Шаблоны, специфичные для приложения «Blogs»

       base.html                  - Базовый шаблон (скелет HTML‑страницы)

       blog_confirm_delete.html   - Форма подтверждения удаления записи

       blog_create.html           - Форма создания новой записи

       blog_detail.html           - Страница с детальной информацией о записи

       blog_list.html             - Страница со списком записей

    includes/                     -Вспомогательные шаблоны (фрагменты HTML)

    static/                       — статические файлы (css, js, image).

catalog/ — приложение для каталога товаров.

    templates/ — HTML‑шаблоны.

    models.py — модели Product, Category, Contact.

    views.py — контроллеры страниц.

    admin.py — регистрация моделей в админке, настройка отображения.

    static/ — статические файлы (css, js, image).

    fixtures/ — фикстуры для загрузки тестовых данных.


.gitignore — список игнорируемых файлов.

requirements.txt — зависимости проекта.

README.md — описание проекта.


###

### На главной странице каталога товаров

Назначение: отображение списка товаров с пагинацией

Основные компоненты:

* Пагинация:

Отображает кнопки навигации между страницами

Стрелки для перехода на предыдущую/следующую страницу

* Список товаров:

Карточки товаров отображаются в адаптивной сетке

На странице показывается 6 товаров

Для каждого товара выводится:

- Изображение

- Название

- Краткое описание (первые 100 символов)

- Кнопка для перехода к детальной странице



###

### На главной Веб‑приложение с блогом

Назначение: отображение списка статей блога

Основные компоненты:

* Пагинация:

Отображает кнопки навигации между страницами


Стрелки для перехода на предыдущую/следующую страницу

* Список статей:

На страницу списка статей выводятся только статьи, которые имеют положительный признак публикации.

На странице показывается 6 статей
Для каждой статьи выводится:

- Изображение

- заголовок статьи

- содержимое (первые 100 символов)



###

### На странице карточки товара

Назначение: отображение подробной информации о товаре

Основная информация:

- Изображение товара с применением фильтра

- Полное название товара

- Детальное описание

- Цена

- Дата создания и последнего обновления

- Принадлежность к категории

- Навигация:
Кнопка возврата к списку товаров



###

### На странице статьи

Назначение: отображение подробной статьи

Основная информация:

- Изображение 

- Заголовок статьи

- Содержание

- Дата создания и последнего обновления

- Навигация:

Кнопка возврата к списку товаров




###

### Модели для магазина с товарами
* Category (категория)

Поля:

name — наименование (строка, макс. 100 символов).

description — описание (текст, макс. 1000 символов).

* Product (товар)

Поля:

name — наименование (строка, макс. 100 символов).

description — описание (текст, макс. 1000 символов).

image — изображение (поле ImageField, загрузка в /image/).

category — связь с Category (ForeignKey, каскадное удаление).

price — цена (десятичное число, 2 знака после запятой).

created_at — дата создания (автоматически).

updated_at — дата последнего изменения (автоматически).

* Contact (контактная информация)

Поля:

phone — телефон.

email — email.

address — адрес.

working_hours — часы работы.


###

### Модели для блога со статьями
* Blog (блог со статьями)

Поля:

title (CharField, до 200 символов) — заголовок записи.

description (TextField, до 1000 символов) — основное содержимое записи.

image (ImageField) — изображение‑превью:

загружается в папку image/ (относительно MEDIA_ROOT);

необязательное поле (blank=True).

created_at (DateTimeField) — дата и время создания записи:

заполняется автоматически при создании (auto_now_add=True).

updated_at (DateTimeField) — дата и время последнего изменения:

обновляется автоматически при каждом сохранении (auto_now=True).

publication (BooleanField) — признак публикации:

    True — запись опубликована;
    
    False — черновик (значение по умолчанию).

counter_views (PositiveIntegerField) — счётчик просмотров:

по умолчанию равен 0 (default=0);

может принимать значение NULL (null=True).



## Установка:

1 Клонируйте репозиторий:
``` git clone git@github.com:lidiar8r-ops/home_work_shop.git  ```

2 Перейдите в папку проекта:
``` cd ваш_репозиторий```

3 Установите зависимости:
``` pip install -r requirements.txt```

4 Примените миграции (создаст таблицы в базе данных):

```
python manage.py makemigrations
python manage.py migrate
```
5 Создайте администратора сайта (чтобы зайти в админку):

```
python manage.py createsuperuser
Введите имя, почту и пароль.
```

6 В терминале запустите сервер:
```python manage.py runserv```

7 Откройте в браузере:

для магазина с товарами 
[http://127.0.0.1:8000/](http://127.0.0.1:8000/)

для блога со статьями 
http://127.0.0.1:8000/blogs/
 
для админки [http://127.0.0.1:8000/admin](http://127.0.0.1:8000/admin)



* через кастомную команду, в терминале запустите 
 
```
python manage.py add_products
```
###

### Дополнительно 

* Главная страница магазина

Выводит 5 последних созданных продуктов (логируется в консоль).

* Страница контактов

Отображает контактную информацию из админки.

Если данных нет — показывает сообщение «Контактные данные не заполнены…»


### Лицензия
можно использовать, изменять и распространять без ограничений.


### Автор
[@lidiar8r-ops](https://github.com/lidiar8r-ops)


Версия: 4.0
Дата: 2026-04-12
